# Machine-specific zsh overrides (paths, API keys, etc.)
# Copy to ~/.zshrc.local — never commit this file.

# export PATH="$HOME/.antigravity/antigravity/bin:$PATH"
# export PATH="$HOME/.opencode/bin:$PATH"
